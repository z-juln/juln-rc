import React, { memo } from 'react';
import { getPrefixCls } from '@/utils';
import './index.less';

export const prefixCls = getPrefixCls('%secName%');

export interface %secName%Props {
  
}

export const %secName%: React.FC<%secName%Props> = ({
  
}) => {

  return (
    <>
      %secName%
    </>
  );
};

export default memo(%name%);
